# Project 2: Tableau

## What this is
Used Tableau Public to dig into subscription data for a fictional SaaS business, 5,000 synthetic customers. Covers retention, revenue retention, and churn.

## Data
The subscription data used across all three dashboards is synthetic. Real subscription data 
from an actual company isn't something you can pull off the shelf for an assignment like this, 
since it's sensitive business data, so it was generated instead using a Python script with 
randomized but realistic values.

- **subscriptions.csv** — generated via `generate_data.py`. Creates 5,000 fake customers with 
  a plan tier, region, signup date, cancellation date, and monthly revenue, plus a downgrade 
  flag and date. Customer tenure is weighted so that most customers churn earlier and fewer 
  stay a long time, similar to how real subscription retention curves usually look. The script 
  uses a fixed random seed, so anyone who runs it gets the exact same 5,000 rows.
- **mrr_history.csv** — generated via `generate_mrr_history.py`. Monthly MRR per customer.
- **nrr_waterfall.csv** — generated via `nrr_waterfall.py`. Pre-calculated New/Expansion/Contraction/Churned totals per month.

## What's in the dashboards

**Cohort Retention Matrix**
- Groups customers by signup month, tracks how many stick around each month after
- Used a WINDOW_SUM table calc to get this right, an LOD approach kept showing 100% retention everywhere which was wrong

**NRR Waterfall**
- Breaks down monthly MRR movement into New, Expansion, Contraction, and Churned
- Numbers reconcile cleanly: for Feb 2024, starting MRR of $17,140 plus $15,770 new plus $740 expansion minus $640 contraction minus $1,350 churned lands exactly on $31,660 ending MRR

**Churn Trends**
- Splits churn into two types and trends both monthly, side by side
- Hard churn (red line): customers whose subscription actually ended
- Soft churn (green line): customers who downgraded but are still active, not gone yet
- Had to add a downgrade_date field to the data generator for this one, the original downgrade flag was just a yes/no with no date attached, so there was nothing to trend it against

## Tool
Tableau
Workbooks are saved as .twbx, screenshots included too so you can see the dashboards without opening Tableau.
